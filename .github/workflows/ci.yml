name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      with:
        fetch-depth: 0
        filter: tree:0

    - name: Setup .NET
      uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
      with:
        dotnet-version: 8.0.x
        cache: false

    # The WinForms demo needs Windows; the library and tests do not.
    - name: Restore
      run: dotnet restore src/Dissimilis.MrzScanner && dotnet restore tests/Dissimilis.MrzScanner.Tests

    - name: Build
      run: dotnet build src/Dissimilis.MrzScanner -c Release --no-restore && dotnet build tests/Dissimilis.MrzScanner.Tests -c Release --no-restore

    - name: Test
      run: dotnet test tests/Dissimilis.MrzScanner.Tests -c Release --no-build --blame-hang-timeout 60s
